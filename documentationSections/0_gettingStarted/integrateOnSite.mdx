---
title: 'Integrate on-site'
description: 'Start delighting shoppers with beautiful on-brand, on-site offers'
---

## Setup options

Integrate gocertify by adding the gocertify script to your site's `<head>` using either a tag manager (no developer needed) or manually pasting it in (basic HTML knowledge recommended). The setup takes less than 2 minutes!

### Use a tag manager (no dev required)
Tag managers let you can easily add the gocertify script site-wide without needing a developer. Pick your prefered manager to see how it's done.

<AccordionGroup>
  <Accordion icon="tag" title="Google Tag Manager">
  Watch our [tutorial video](tutorial.com) or follow these steps.

  <Steps>
    <Step title="Add a new tag">
      Navigate to your [Google Tag Manager](https://tagmanager.google.com/#/home) (GTM) account and select `Add new Tag`
    </Step>
    <Step title="Pick the Custom HTML configuration">
      Within the tag configuration, select the `Custom HTML` option.
    </Step>
    <Step title="Paste in your gocertify script">
      Copy and paste the following script into the HTML tag. Remember to replace `exampleCompany` with your actual company name.
      ```javascript gocertify-script
        <script>
          (function() {
            var el = document.createElement('script');
            el.setAttribute('src', 'https://assets.gocertify.me/assets/embed.js');
            //replace exampleCompany with your company name
            el.setAttribute('data-location', '/at/exampleCompany'); 
            document.body.appendChild(el);
          })();
        </script>
      ```
    </Step>
    <Step title="Trigger to fire on all pages">
      Within the `Triggers` options configure the tag to fire on all pages. 
      <Info>
        **Note**: The script is just 12kb and well optimised so it won't slow down your site.
      </Info>

    </Step>
    <Step title="Save and Publish">
      Save your tag as `gocertify` and publish your container.
    </Step>
  </Steps>


    
  </Accordion>
  <Accordion icon="network-wired" title="Awin Mastertag">
    Watch our [tutorial video](tutorial.com) to see how it's done.

    <Steps>
      <Step title="Add the gocertify plugin">
        Navigate to the Awin Plugin Store, search for gocertify and click `Add to Account`.
        <Frame>
          <img src="/images/awinFeatured.png" alt="Awin gocertfiy Plugin" />
        </Frame>
      </Step>
      <Step title="One click activate">
        Navigate to your Mastertag Toolbox, and activate the gocertify plugin.
      </Step>
    </Steps>
  </Accordion>
</AccordionGroup>

### Or Manually add script

<AccordionGroup>

<Accordion icon="rectangle-terminal" title="Add script to head">
  
  Copy and paste the gocerty script into the `<head>` of your site. Remember to replace `exampleCompany` with your actual company name.
  ```javascript gocertify-script
    <script>
      src="https://assets.gocertify.me/assets/embed.js" 
      //replace exampleCompany with your company name
      data-location="/at/exanpleCompany"
    </script>

  ```
  <Info>
    **Note**: The script is just 12kb and well optimised so it won't slow down your site.
  </Info>
</Accordion>

</AccordionGroup>

